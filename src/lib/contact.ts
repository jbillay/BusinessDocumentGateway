/**
 * Public contact address used by the marketing site (footer + contact form).
 * Swap this for a branded support inbox (e.g. support@yourdomain) once the
 * custom domain is live (task 019).
 */
export const CONTACT_EMAIL = 'jbillay@gmail.com'
