/**
 * Public contact address used by the marketing site (footer + contact form).
 * Placeholder until the custom domain is live (task 019) — swap for the real
 * branded inbox and wire the mailbox up before launch.
 */
export const CONTACT_EMAIL = 'contact@bdg.com'
